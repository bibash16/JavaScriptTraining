// function(param1, param2 ){

// }

function add(a, b) {
    //console.log(a+b);
    return (a + b)

}
// add(2,4);  // you have to call the function otherwise the insides
//of the fucntion wont work
let result = add(2, 4);
console.log(result);

/*x += 5 would mean x = x + 5 ( where a value of x is 
    already present)
    the same applies to -= *= /= 
    */

